let password = "Marcisamazing";
let passwordLength = password.length;
console.log (`there is ${passwordLength} characters`);

if (passwordLength< 8) 
{ console.log(`Your password is ${passwordLength} characters, that is too short`)


}
else{

    console.log('Your password is the correct length')
}

